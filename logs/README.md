# logs / 原始日志与飞行数据

存放测试产出的**原始日志、飞行记录、录制**的索引与归档指引。默认**不提交**二进制到仓库（见根 `.gitignore`）。

建议：
- 每次测试的 `*.bin` 飞行日志、`mlog` 输出、录制影像，按 `logs/raw/YYYY-MM-DD_主题/` 归档（本仓库忽略 raw 目录，避免体积与泄密）。
- 本目录放：日志索引说明、DataSIM 分析结论摘要、云端/网盘的存放链接（仅内部可见时注明访问范围）。
- 分析结论（而非原始数据）应沉淀到 `tests/` 与 `issues/`。